import {CommonEntity} from "../../common/entities/common.entity";

export class UserEntity {
    name: string
    email: string
    password: string
    role: string
    verified: boolean
    age: number
    gender: string
    address: string
}